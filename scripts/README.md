# Scripts

This folder is the Foundry module script folder.

Expected files:

- `debt-collectors.js`
- `warrenSkinCloak.js`

These are referenced from the root `module.json`.

The working source files should come from the current `sanctoria-automation-v0.7.0` package until they are committed here directly.
